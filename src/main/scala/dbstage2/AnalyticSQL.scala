package dbstage2

class Relation
class Query

object AnalyticSQL {
  /*
  def From(sch0: Schema)(f: Relation => Query)
  def From(sch0: Schema, sch1: Schema)(f: (Relation, Relation) => Query)
  def From(sch0: Schema, sch1: Schema, sch2: Schema)(f: (Relation, Relation, Relation) => Query)
  
  def SELECT (cols: Column*): Query = ???
  */
  
}
